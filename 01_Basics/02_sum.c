/* 
 Program to calculate the sum of two numbers
*/

#include <stdio.h>

int main() {
    int a, b;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Display sum
    printf("The sum is: %d\n", a + b);

    return 0;
}